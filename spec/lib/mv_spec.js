
'use strict';

describe('mv', function() {

  var mv = require('../../lib/mv');

  it('moves data recursively from one ref to another', function() {

  });

  it('fails if data cannot be recursively merged', function() {

  });

  it('does not delete the source if the copy phase fails', function() {

  });

});
